#include <stdlib.h>
#include <stdio.h>

/*void append(){
	int x = 10;
	if(count == 4)
		buffer[nextin] = x;
	nextin = (nextin + 1) % N;
	count++;

	signal(notempty);
}

void take (char x){
	if(count == 0)
		wait(notempty);
	x = buffer[nextout];
	nextout = (nextout + 1)% N;
	count--;

	signal(notfull);

}
*/
void test(int num){
	printf("in library %d",num);
}

